export const meta = {
  title: 'MDX 🔥'
}

import Button from '../component/Button'

# What is it?

[MDX](https://mdxjs.com/) is a format that lets you seamlessly use JSX in your Markdown documents.

It supports normal markdown content such as codeblocks:

```js
var foo = 42;
```

As well as React components:

<Button>Click me</Button>
