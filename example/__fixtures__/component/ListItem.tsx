import * as React from 'react';

type Props = {
  /**
   * Title text of the list item
   */
  title: string;
};

/**
 * List.Item allows you to show some content in a list.
 */
export default class ListItem extends React.Component<Props> {
  static displayName = 'List.Item';

  render() {
    return null;
  }
}
